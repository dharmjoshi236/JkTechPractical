# Configure this project in local system

1. Go to the root directory
2. In terminal, Hit command `npm install`
3. Then when all the node modules are installed, then hit `node start` to start the project
